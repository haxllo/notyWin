<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="NotyWin.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:NotyWin.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="NotyWin"
    mc:Ignorable="d">
    <Grid x:Name="RootGrid" Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="HeaderText" Text="NotyWin — running"
                   FontSize="20" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" x:Name="DisplaysText" Text="Displays: ..."
                   Margin="0,8,0,0" />
        <TextBlock Grid.Row="2" x:Name="NotesText" Text="Notes: ..."
                   Margin="0,4,0,0" />
        <TextBlock Grid.Row="3" x:Name="HelpText"
                   Text="Move cursor to the right edge of a display to fan the deck. Right-click a tab for the menu. Hover to peek."
                   Margin="0,12,0,0" TextWrapping="Wrap" MaxWidth="500" HorizontalAlignment="Left" />
    </Grid>
</Window>
